using System;
using UnityEngine;
using System.Reflection;
using UnityEditor;
//using ElephantSDK;
public class ElephantWrapper : MonoBehaviour
{
    /* private void Start() {
        GameManager.Instance.OnLevelStarted.AddListener(ElephantLevelStarted);
        GameManager.Instance.OnLevelFailed.AddListener(ElephantLevelFailed);
        GameManager.Instance.OnLevelSuccess.AddListener(ElephantLevelCompleted);
    }
    private void ElephantLevelStarted(int level) => Elephant.LevelStarted(level);
    private void ElephantLevelCompleted(int level) => Elephant.LevelCompleted(level);
    private void ElephantLevelFailed(int level) => Elephant.LevelFailed(level); */
}